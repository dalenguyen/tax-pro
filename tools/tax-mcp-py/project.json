{
  "name": "tax-mcp-py",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "tags": ["scope:tools", "type:mcp"],
  "targets": {
    "deploy": {
      "command": "gcloud run deploy tax-mcp-py --source tools/tax-mcp-py --region us-central1 --quiet",
      "options": {
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
